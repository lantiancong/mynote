## 80

## 242

```c++
// 对 [first, last) 范围内的元素进行升序排序
sort(first, last);
sort(s.begin(), s.end());
```

![](.\img\1.png)

## 14

> Math.min(int,int)
>
> String.chatAt(index)
>
> String.substring(开始,结束)

# 209

# 3

HashSet<>()

.charAt()

对指针的处理

# 76

toCharArray()