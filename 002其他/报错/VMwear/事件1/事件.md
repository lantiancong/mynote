事件的经过是这样的

我导入了Escalate靶机

结果一直提示找不到log文件

我以为是我从B站下载的VMware的问题，于是打算卸载重装

（但后面发现，只要改一下Escalate的兼容问题就行，在虚拟机 =》管理 =》兼容性里，改成16就行了）

**于是问题就出现了**

这也是我之前一直不知道的坏习惯

我删软件都是直接从我最早自定义的安装目录直接整个删除

然后这一次，让我真正意识到前段时间上的注册表的关键作用

如果程序自带或用电脑设置里卸载，是会连下载时添加的注册表信息一起删的

**但是这次，VMware暴露了我的问题**

删除后本来不能用，但修复两次后（用设置中的修改修复，VMware特性）可以用了

**但是，我虚拟网卡不见了**

也就是说在VMware中是正常的，但外部无法访问虚拟机（访问靶机web服务时发现的）

于是，我去B站找办法，说是没删干净，用了geek和ccleaner，

**然后，重装不了了。。**

最后是在一篇会员CSDN博客中找到了解决办法

就是用工具把残余删干净（Windows install Clean up）